import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-react-todo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
